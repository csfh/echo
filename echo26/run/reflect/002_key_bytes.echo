; Int `1` and string `"1"` key material must not share a prefix.
/ std/io
/ std/str
/ std/bytes
/ std/reflect

io.print(str.from_int(bytes.len(reflect.key_bytes(1))))
io.print(str.from_int(bytes.len(reflect.key_bytes("1"))))
