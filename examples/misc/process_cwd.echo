; process.run_cwd pwd in /tmp. Run: xo run examples/misc/process_cwd.echo
/ std/io
/ std/str
/ std/process

| process.run_cwd("pwd", [], "/tmp") {
    $ r {
        io.print(str.from_int(str.contains(r.stdout, "tmp")))
    }
    ! e {
        io.print(e)
    }
}
